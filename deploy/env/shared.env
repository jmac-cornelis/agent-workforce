# ==========================================================================
# shared.env — Non-sensitive configuration shared across all agents
# ==========================================================================
LOG_LEVEL=INFO
STATE_BACKEND=json
PERSISTENCE_DIR=/data/state
SHANNON_STATE_DIR=/data/shannon
HEMINGWAY_DOC_DIR=/data/hemingway/docs
DRY_RUN=true
